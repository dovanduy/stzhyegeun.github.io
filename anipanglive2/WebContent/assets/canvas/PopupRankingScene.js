// Generated by Phaser Editor v1.4.0 (Phaser v2.6.2)


/**
 * PopupRankingScene.
 * @param {Phaser.Game} aGame A reference to the currently running game.
 * @param {Phaser.Group} aParent The parent Group (or other {@link DisplayObject}) that this group will be added to.    If undefined/unspecified the Group will be added to the {@link Phaser.Game#world Game World}; if null the Group will not be added to any parent.
 * @param {string} aName A name for this group. Not used internally but useful for debugging.
 * @param {boolean} aAddToStage If true this group will be added directly to the Game.Stage instead of Game.World.
 * @param {boolean} aEnableBody If true all Sprites created with {@link #create} or {@link #createMulitple} will have a physics body created on them. Change the body type with {@link #physicsBodyType}.
 * @param {number} aPhysicsBodyType The physics body type to use when physics bodies are automatically added. See {@link #physicsBodyType} for values.
 */
function PopupRankingScene(inIsWin, aGame, aParent, aName, aAddToStage, aEnableBody, aPhysicsBodyType) {
	
	Phaser.Group.call(this, aGame, aParent, aName, aAddToStage, aEnableBody, aPhysicsBodyType);
	this.game.add.sprite(0, 0, 'popupLarge', 'popup_large.png', this);
	
	var _maskingGroup = this.game.add.group(this);
	_maskingGroup.position.setTo(0, 115);
	
	var _itemList = this.game.add.group(_maskingGroup);
	
	var _btnClose = this.game.add.sprite(574, 25, 'popupLarge', 'x_btn.png', this);
	
	// public fields
	
	this.fMaskingGroup = _maskingGroup;
	this.fItemList = _itemList;
	this.fBtnClose = _btnClose;
	/* --- post-init-begin --- */
	
	// ?�� ?���? 추�?
	window.MeInfo.friends.push({
		"id": window.MeInfo.real_id, 
		"name": window.MeInfo.name, 
		"platform_id": window.MeInfo.id,
		"thumbnail": window.MeInfo.thumbnail,
		"trophy": (inIsWin ? securityStorage.getInt('winTrophy') : securityStorage.getInt('loseTrophy'))
	});
	
	// 친구?���? ?��?��
	StzUtil.quickSort(window.MeInfo.friends);
	
	// �? 10개의 ?��?��?���? 뽑아?�� - 리뷰?��?�� 그냥 ?���? ?��?��
	
	// 10개의 ?��?��?�� 각각 ?��리팹 ?��?��?�� ?��?��
	for (var index = window.MeInfo.friends.length - 1; index >= 0; index--) {
		var currentItem = new PopupRankingItem(aGame, _itemList);
		currentItem.setData(window.MeInfo.friends.length - index, window.MeInfo.friends[index]);
		currentItem.position.setTo(0, 180 * (window.MeInfo.friends.length - index - 1));
	}
	
	// ?��?��?�� ?��?��?���? ItemList 그룹?�� 붙임.
	
	// ItemList 그룹?�� ?��?��?��?��.
	
	
	/* --- post-init-end --- */
	
	
}

/** @type Phaser.Group */
var PopupRankingScene_proto = Object.create(Phaser.Group.prototype);
PopupRankingScene.prototype = PopupRankingScene_proto;
PopupRankingScene.prototype.constructor = PopupRankingScene;

/* --- end generated code --- */
// -- user code here --
